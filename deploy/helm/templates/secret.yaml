apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
stringData:
  TEACH_SECRET: {{ .Values.env.TEACH_SECRET | quote }}
  {{- if .Values.llm.apiKey }}
  LITELLM_API_KEY: {{ .Values.llm.apiKey | quote }}
  {{- end }}
